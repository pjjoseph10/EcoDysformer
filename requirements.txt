# Eco-Dysformer v2 -- Stage 1 pinned dependencies.
#
# This set is pinned for reproducibility and is tested against the Kaggle
# P100/T4 base image (Python 3.10/3.11), which is the documented GPU target.
# On Kaggle, torch / numpy / pandas / scikit-learn / lightgbm are already
# preinstalled at compatible versions; the pins below are the reference env.
#
# Enable "Internet" in the Kaggle notebook: stanza, udpipe, transformers, and
# wordfreq download models/tables on first use.

# --- core numerics ---
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1
scikit-learn==1.5.1
pyyaml==6.0.1

# --- deep learning (RQ1 Performer / quadratic arms, fusion) ---
torch==2.3.1

# --- downstream classifier ---
lightgbm==4.4.0

# --- Czech linguistic pipeline (choose one via config; both pinned) ---
ufal.udpipe==1.3.1.1
stanza==1.8.2
wordfreq==3.1.1

# --- supplementary embeddings (OFF by default, Stretch priority 3) ---
transformers==4.42.4
tokenizers==0.19.1

# --- explainability ---
lime==0.2.0.1

# --- plotting / utilities ---
matplotlib==3.8.4
tqdm==4.66.4
tabulate==0.9.0          # markdown tables (baseline comparison)

# --- tests (dev) ---
pytest==8.2.2
